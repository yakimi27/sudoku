<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Sudoku.Presentation.Views.Controls.SudokuCellControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid
        Width="60"
        Height="60"
        Background="{Binding State, Converter={StaticResource CellStateToColorConverter}}"
        BorderBrush="#CCCCCC"
        BorderThickness="1">

        <VisualStateManager.VisualStateGroups>
            <!-- Selection State -->
            <VisualStateGroup x:Name="SelectionStates">
                <VisualState x:Name="Unselected"/>
                <VisualState x:Name="Selected">
                    <Storyboard>
                        <ColorAnimation
                            Storyboard.TargetName="SelectionBorder"
                            Storyboard.TargetProperty="BorderBrush.Color"
                            To="#0078D4"
                            Duration="0:0:0.1"/>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>

            <!-- Highlight State -->
            <VisualStateGroup x:Name="HighlightStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="Highlighted">
                    <Storyboard>
                        <ColorAnimation
                            Storyboard.TargetName="BackgroundRectangle"
                            Storyboard.TargetProperty="Fill.Color"
                            To="#E8F4FF"
                            Duration="0:0:0.1"/>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>

            <!-- Error State -->
            <VisualStateGroup x:Name="ErrorStates">
                <VisualState x:Name="NoError"/>
                <VisualState x:Name="HasError">
                    <Storyboard>
                        <ColorAnimation
                            Storyboard.TargetName="BackgroundRectangle"
                            Storyboard.TargetProperty="Fill.Color"
                            To="#FFE8E8"
                            Duration="0:0:0.1"/>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Rectangle
            x:Name="BackgroundRectangle"
            Fill="White"/>

        <Border x:Name="SelectionBorder" BorderThickness="3" BorderBrush="Transparent"/>

        <Grid Padding="2">
            <!-- Large value display -->
            <TextBlock
                x:Name="ValueText"
                Text="{Binding Value}"
                FontSize="28"
                FontWeight="Bold"
                TextAlignment="Center"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding Value, Converter={StaticResource ValueToVisibilityConverter}}"/>

            <!-- Notes grid (3x3) -->
            <Grid x:Name="NotesGrid" Visibility="{Binding Notes, Converter={StaticResource NotesVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Note digits 1-9 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="1" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=1}"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="2" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=2}"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="3" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=3}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="4" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=4}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="5" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=5}"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="6" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=6}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="7" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=7}"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="8" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=8}"/>
                <TextBlock Grid.Row="2" Grid.Column="2" Text="9" FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes, Converter={StaticResource NoteSetToVisibilityConverter}, ConverterParameter=9}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
